
mutation CreateUserTest($input: UserInput!) {
    user {
        create(input: $input) {
            id
        }
    }
}

query CurrentUser {
    user {
        currentUser {
            id
        }
    }
}
